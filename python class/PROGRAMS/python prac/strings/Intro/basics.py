# # recall in numerical inputs we use the eval  statement but here in string we do not     example bellow
num = eval (input("Enter a number: "))
string = input ("enter a string: ")
"""there is also a way to get the number of chars in a string using the built in function len

for example
"""
len("Hello")